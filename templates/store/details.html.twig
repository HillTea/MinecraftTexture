{% extends 'base.html.twig' %}

{% block title %} {{ title }} {% endblock %}

{% block main %}
    {% include 'partials/_header.twig' %}
{% endblock %}

{% block body %}

    <main class="pt-5 d-flex flex-wrap justify-content-center gap-5">
        <article class="col-12 col-lg-6 bg-white p-4">
            {#            Favoriser le SEO de la page #}
            <div class="col-12 d-flex">
                <div class="col-8 d-flex flex-wrap">
                    <img class="rounded" width="75" height="75" src="{{ product.pathLittleImage }}" alt=""/>
                    <h1 class="ps-2 fs-3">{{ product.name }}</h1><br>
                    {#                    <p class="small text-muted mt-3">{{ product.review }} | {{ product.price }}</p> #}
                </div>
                <div class="col-4 text-end">
                    {% if users.contains(app.user) and app.user is not null %}

                        <button class="btn btn-primary">
                            Download
                        </button>
                    {% else %}

                        <button class="btn btn-primary">
                            Price {{ product.price }}€
                        </button>

                    {% endif %}
                </div>
            </div>
            <div class="col-12 pt-5">
                {{ product.description|raw }}
            </div>
        </article>
        <aside class="col-12 col-lg-2 text-center rounded h-100">

            {#            <div class="col-12 mt-3"> #}
            {#                <div class="col-12"> #}
            {#                    <img src="{{ product.seller.pathImage }}" class="rounded-circle mb-3 mt-3 avatar border" height="150" #}
            {#                         width="150" #}
            {#                         alt="{{ product.seller.pseudo }}"/> #}
            {#                </div> #}
            {#                <a href="{{ path('app_account', {'pseudo': product.seller.pseudo}) }}" class="fs-4 text-decoration-none text-dark">{{ product.seller.pseudo }}</a> #}
            {#            </div> #}
            {#            #}
            <div class="card mb-4">
                <div class="card-body text-center">
                    <img src="{{ product.seller.pathImage }}" alt="{{ product.seller.pseudo }}"
                         class="rounded-circle img-fluid" style="max-width: 75px; min-width: 75px; max-height: 75px; min-height: 75px;">
                    <h5 class="my-3">{{ product.seller.pseudo }}</h5>
                    <div class="d-flex justify-content-center mb-2 mt-5">
                        <a href="{{ path('app_account', {'pseudo' : product.seller.pseudo}) }}#ressources"
                           class="btn btn-primary btn-sm">Ressources</a>
                    </div>
                </div>
            </div>
        </aside>
    </main>

{% endblock %}
